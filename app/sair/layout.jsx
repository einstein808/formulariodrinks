export const metadata = {
  title: 'Opt-out | Laboratório de Drinks',
  robots: {
    index: false,
    follow: false,
  },
};

export default function SairLayout({ children }) {
  return children;
}
