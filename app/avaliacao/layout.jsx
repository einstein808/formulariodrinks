export const metadata = {
  title: 'Avaliação | Laboratório de Drinks',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AvaliacaoLayout({ children }) {
  return children;
}
